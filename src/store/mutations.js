

export const SET_USERS = (state, users) => {
  state.users = users
}
export const SET_COMMENTS = (state, topComments) => {
  state.topComments = topComments
}
export const SET_NEWS = (state, news) => {
  state.news = news
}
// export default { SET_USERS, SET_COMMENTS };
